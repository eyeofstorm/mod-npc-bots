DELETE FROM `npc_text` WHERE `ID` IN (9000000, 9000001, 9000002);

INSERT INTO `npc_text` (`ID`, `text0_0`) VALUES
(9000000, `There are always dudes ready to kill for money.`), 
(9000001, `Mercenaries are always in demand. Here is what available right now.`),
(9000002, `Mercenaries are always in demand. Here is what available right now.`);
